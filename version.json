{
  "version": "phantom-v1.14.539",
  "released": "2026-08-30",
  "prevVersion": "phantom-v1.14.538",
  "notes": "v1.14.539 — THE FIRST-RUN ORPHANS COME OUT. Cleanup ship closing the debt v1.14.474 left behind when its 2-field cold open removed the gate's SITE LEAD input and Master step but kept the code behind them. Three things go: the dead reader in firstRun_confirm that guarded on #fr-siteLead, an element absent since .474; firstRun_loadMaster, which had ZERO callers; and firstRun_masterLine, whose only caller was firstRun_loadMaster, so it was orphaned transitively — a second-order orphan that a one-level check would have missed. 51 lines, 2,773 bytes. ⛔ Three tests drove this cluster and PASSED, which is worse than no coverage: a green test against unreachable code reads as maintenance. They collapse into one that pins the cleanup — the Master step is gone from the gate, both orphans are undefined, and the ONE import door master_loadFromPicker still exists. ⚠ Two of the deleted tests asserted Master-line HONESTY (never invent a count). That rule is NOT relaxed; it is Contract 10 and still binds every surface that reports Master state. What changed is that this particular surface no longer exists. Also corrected: the ad-hoc verifier used in this session mislabelled its brace tally as 'CSS braces' — a stray '<style' inside a JS string opened a phantom style span that swallowed JS braces, so the real CSS figure is 2 blocks / 4018 braces, not 12 / 4848. The balance conclusion was right every time; the label and magnitude were not."
}
