{
  "version": "phantom-v1.14.565",
  "released": "2026-09-01",
  "prevVersion": "phantom-v1.14.564",
  "notes": "v1.14.565 — THE DRAWINGS LEAVE THE DEFAULT PATH. RACK-ELEVATION-DEMOTE Ship 2, sliced into 2a and 2b by owner ruling; this is 2a, the visible half. Rack detail no longer renders the elevation card or the full-height minimap strip. ⭐ THE INFORMATION STAYED AND SHIPPED FIRST: .564 put the U position on every DEVICES row, sorted top-of-rack down, open by default, with an em dash where the Master records none — so the list carried the fact for one VERIFIED ship before the picture left. That overlap is the whole point of the slicing and it is now spent. WHAT WENT: the .rack-hybrid wrapper, both .rack-hybrid-canvas branches, the .rack-hybrid-minimap scrubbar, and the double-rAF driving rackHybrid_initSync, rackFlat_applyFit and railSel_bindFlat. OPEN AISLE is untouched and is now the only rack-picture door on the surface. ⛔ WHY 2a AND 2b ARE SEPARATE SHIPS: S-6 says no dead code left behind, but taken in one stroke that meant deleting four functions, a CSS block and resolving five external guards in the same ship as a visible surface removal, on a 60k-line file with no type checker. 2a is the visible change and is trivially revertible; 2b is the deletion, has NO visible change, and is provable by grep. ⛔ 2b's DEBT IS RECORDED IN THE SOURCE, NOT ONLY IN A DOC: rackHybrid_initSync, railSel_bindFlat, rackFlat_applyFit and scrubbar_buildHtml are now orphaned — each had exactly one caller and it was here — and #reh3dCanvasHost is gone from the DOM while five external paths still look it up (19619, 19650, 22189, 23719, 39244). Those are null-guards and fail safe, but they are permanently-null lookups, which is the .473 shape, and 2b resolves them. ⚠ WHAT IS NOT DEAD: rackElevation_buildHtml stays live for the Command hero (23705) and RackEngine.renderFlat (38841), which is under R-05; both rely on the --u-h CSS default the renderer emits at 40927, and rackFlat_applyFit only ever overrode it with a measured value, so losing it costs them nothing. S-14's zero-reference grep gate remains unreachable while those two callers exist and is NOT attempted. ⚠ S-8's clipped U48 label is resolved BY REMOVAL as the spec says, and the open question of whether that label lived on the elevation or the strip is now MOOT — both halves are gone. Do not reopen it as a bug."
}
