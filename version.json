{
  "version": "phantom-v1.14.560",
  "released": "2026-09-01",
  "prevVersion": "phantom-v1.14.559",
  "notes": "v1.14.560 — SIX SILENTLY DEAD DECLARATIONS IN THE OPS ROW. `--text-primary` and `--text-tertiary` are declared NOWHERE in this file — zero occurrences — and an undefined var() invalidates its entire declaration in silence. So six `color` rules (.ops-banner-btn, .ops-grid-back, .ops-grid-title, .ops-panel-name, .ops-panel-meta, .ops-panel-label) were inert and every one of those elements inherited colour from its parent instead. ⛔ THIS DEFECT IS NOT NEW AND THAT WAS PROVEN, NOT ASSUMED: the dead-token set is byte-identical at .556 — same four tokens, nothing added, nothing fixed. ⭐ WHAT IS NEW IS THAT ANYONE CAN SEE IT. The rules shipped in .465 and sat inert for 85 versions because ops_init never ran; .558 restored the row and .559 added a tenth panel, so the dead colour finally renders. The defect is old; the exposure is mine. ⭐ OWNER RULING on the replacements: --white for the four primary sites, --slate-dim for the two dim ones. ⚠ THE opacity IS REMOVED IN THE SAME STROKE, and that is the owner's option 2, not a drive-by: --slate-dim is already the dim step of the palette, so .ops-panel-meta's opacity:.6 and .ops-panel-label's opacity:.7 were dimming twice, computing to roughly 2.2:1 against --surf-1 at 9-10px — under the Cold Aisle bar for a loud dark aisle. ⚠ SCOPE, STATED SO THIS SHIP IS NOT MISREAD: this changes colour, not geometry, so it does NOT close 44-tool-reachability's gloved floor, which fails identically at .556. Nor does it touch 06-composition's contrast floor, whose assertion is on #rd-botnav .blabel — a different element, pinned, and also failing at .556. ⭐ 19-design-tokens is the only gate in this project that can see a silently-killed CSS declaration — brace balance passes and node --check never reads CSS. It found --alert the same way at .428. Dead tokens go 4 to 2; the survivors (--tac, --red-neon) are both inert."
}
