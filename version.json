{
  "version": "phantom-v1.14.360",
  "released": "2026-07-30",
  "prevVersion": "phantom-v1.14.359",
  "notes": "SHIP v1.14.360 - THE TWO HELD BANNERS RELEASE. Adds the DEPLOY and HANDOFF <img> references to their .359 banner rows, so all five BUILD rows now carry a photograph. Two owner rulings 2026-07-30 unblocked this: B5 = ACCEPT ('the app is site-agnostic but the art does not have to be'), so the DEPLOY raster commits as-is with GB300 / NVL72 branding legible on the faceplates - CLOSED, do not re-raise. Section 5.5 = WAIVED FOR THE HANDOFF BANNER ONLY, so its legible baked workflow text ships as owner-accepted debt; this is LOGGED AND EXPLICITLY NOT A PRECEDENT - 5.5 stays in force for every future banner render, and this row must not be cited as precedent by a later one. SCOPE IS TWO ASSET REFERENCES AND NOTHING ELSE. The banner rows, the OPS wall, the handlers, the registry and the CSS that landed in .359 are all untouched - the entire dct-ios.html diff is the two <img> tags, the comment block that documented the hold (rewritten to record the rulings that replaced it), and the version stamp. FILENAME DEVIATION, REPORTED NOT SILENTLY RESOLVED: the ruling named the deploy asset 'DEPLOY-phone@3x.webp'. No such file exists in the repo, in Downloads, or in git history. What .357 actually shipped is icons/phantom-banner-deploy-1170.webp, and the ruling's own sentence - 'both files are already in the repo from .357 and .358' - identifies that file, so THAT is what is referenced here. Nothing was renamed, re-staged, re-encoded or re-cropped, per the ruling. CONSEQUENCE THE OWNER SHOULD KNOW: the stated acceptance check `grep -c 'DEPLOY-phone' dct-ios.html` returns 0, because satisfying it literally would require renaming a raster that shipped three versions ago and rewriting its precache entry - which the same ruling forbids. If the DEPLOY-phone name is wanted, it is a one-line rename plus a precache line in a follow-up ship. PRECACHE_URLS NEEDED NO CHANGE: both rasters were already registered by .357 (line 123) and .358 (line 124) when they were staged, so references and precache membership are consistent in this commit without touching the list. sw.js carries the CACHE_VERSION bump only. The .bnr hatch fallback STAYS in the CSS by explicit instruction - it is the standing mechanism for any future missing asset, not scaffolding for these two rows, and it is now dormant rather than dead. ASSETS VERIFIED BEFORE REFERENCE: both files are on disk, both decode at exactly 1170x403 (aspect 2.90323), identical geometry to the three already shipped, so the width/height attributes match their intrinsic size and no row will shift. deploy 42.1KB, handoff 36.7KB. GATES: three-stamp lockstep; inline JS compiles (4 of 4 blocks) plus sw.js; CSS braces balanced in all 12 style blocks; diff surgical; the .359 acceptance checks re-run green (5 banner rows, 9 wall cells); every referenced raster exists on disk and is precached; no invented value present anywhere. DEVICE-VERIFY: (1) badge reads v1.14.360. (2) BUILD shows five rows and ALL FIVE now carry a photograph - the diagonal accent hatch should be gone from DEPLOY and HANDOFF. (3) Every row still taps through and all nine OPS wall cells still open their tool - nothing about the handlers changed, but this is the cheap regression sweep. (4) CLEAR THE SERVICE WORKER, airplane mode, hard reload - all five photographs must still render offline. This is the real gate and it now exercises the full 247KB banner set for the first time. (5) ?legacy=1 unchanged. ROLLBACK: revert this commit and the two rows return to the hatch fallback with everything else in .359 intact - the holds were the only thing this ship changed."
}
