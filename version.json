{
  "version": "phantom-v1.14.551",
  "released": "2026-08-30",
  "prevVersion": "phantom-v1.14.550",
  "notes": "v1.14.551 — LEGACY-RETIRE STAGE 6.6: CAGE NUT COMPASS IS DECOUPLED, and it was the only re-home that SYNTHESISED markup rather than moving it. redesign_homeCompass built the #pw-compass panel and the Cage Nut stab at boot with createElement; neither existed in any source. Both are now authored inside #rf-hw, and pw-compass is a declared member of STAB_GROUPS[pwr-sub] instead of a runtime push — the group and the markup it governs are finally declared in one place. Three details had to be read out of the function because nothing recorded them: the panel class is ref-sub (NOT pwr-sub, despite joining that group), the .cn-header Back-to-Triage bar is deliberately dropped because the subtab strip is the chrome, and the stab carries both showStab and compass_render() in its onclick. ⭐ Its ordering dependency on redesign_homeHardware — it appended into a strip that only existed in #rf-hw once Hardware had moved it — was already gone: Stage 6.5 authored that strip in source, so 6.6 removed a function whose precondition had stopped mattering a ship earlier. ⛔ VISIBLE UNDER ?legacy=1: the legacy house loses the Cage Nut Compass, the seventh organ. goCompass legacy branch now says so rather than deferring compass_render into a hidden node on top of a page that no longer exists. The orphaned #pg-compass CSS rule went with the page. ONE ORGAN LEFT: pg-scan."
}
