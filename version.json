{
  "version": "phantom-v1.14.535",
  "released": "2026-08-30",
  "prevVersion": "phantom-v1.14.534",
  "notes": "v1.14.535 — LEGACY-RETIRE STAGE 2b: THE TODAY PULSE CARD IS REMOVED, and this one is NOT dead code. The pulse card — site label, deployment name, a four-stat row (racks / done / active / blocked) and an overall progress bar — rendered under ?legacy=1 as part of the legacy NOW dashboard. It never rendered under body.rd, because today_render paints into #today-dashboard inside pg-triage and the redesign never shows that page. So this is a VISIBLE change in the legacy house, shipped under the 2026-08-29 revocation of Contract 17's byte-identity guarantee, and the census row calling it RETIRED under a definition of 'no working entry point anywhere' was wrong. 30 lines, 2,765 bytes. Its two locals (contextName, contextSite) were used nowhere else, and its 14 opening divs were matched by 14 closers, so the surrounding html string stays balanced. ⚠ today_render itself is untouched and still has four callers, one at boot — it runs in full on every launch and paints into a host the redesign never displays. That is a dead-render-host finding that outlives this stage and belongs to no ship yet."
}
