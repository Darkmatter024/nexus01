{
  "version": "phantom-v1.14.563",
  "released": "2026-09-01",
  "prevVersion": "phantom-v1.14.562",
  "notes": "v1.14.563 — THE RETIRED RAIL'S LAST WRITE. Dead-code removal only; nothing renders differently and no surface moves. v1.14.321 retired the .159 sliding reactor rail in a wholesale owner-approved nav replacement, but retired it by HIDING it: the <span id=\"bn-core\"> stayed in the markup under #bn-core{display:none}, and showMode kept writing _core.style.left = index * 33.333% on EVERY mode change for 242 versions. ⛔ THE NUMBER WAS ALSO WRONG FOR THE FUTURE: thirds math against a nav that Contract A8 takes to five pillars, where the step is 20%. Inert, invisible, and never throwing — the same shape as .473, where a stated intent the bytes did not carry sat undetected for 85 versions. ⭐ FOUND BY READING, NOT BY FAILING: no test was red and no user could see it. WHAT SHIPS: the span, the display:none rule, the three lines of JS (_bnIdx had no other consumer) and one stale comment. ⚠ ORDER WAS THE RISK AND IT RUNS AGAINST INTUITION — #bn-rail is display:contents, so its children are promoted into #rd-botnav's grid-template-columns:repeat(4,1fr). #bn-core stayed out of that count ONLY because it was display:none, which made the rule load-bearing while the span existed; dropping the rule alone would have made it a fifth grid item and collapsed the nav. Span first, then rule, in one edit. 01-nav's test that pinned the dead write is replaced by one that pins its ABSENCE and asserts #rd-botnav still resolves to exactly four grid items — that second assertion is the one that would have caught the ordering trap. Rules the owner took this session: the A3 sliding thumb is REJECTED (.btick stays), so this element had no future to wait for; and board item O-2c is DECLINED — ISOLATE was traced and performs no destructive operation, so it keeps its place in the OPS row."
}
