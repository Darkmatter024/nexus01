{
  "version": "phantom-v1.14.354",
  "released": "2026-07-25",
  "prevVersion": "phantom-v1.14.353",
  "notes": "SHIP v1.14.354 - SHIP B edit 9 (the B4.3 reflow, held from .353) + the OPEN AISLE tap-target bump. Two owner rulings (John, 2026-07-25): (a) move the 3D elevation to just below NEXT ACTION; and bump the OPEN AISLE door to a 44px gloved tap-target. FIRST ATTEMPT WAS CAUGHT BY THE GATES AND REDONE: a naive whole-block move reordered the LEGACY rack-detail page too, because deploy_showRackDetail is a SHARED function (deploy_opsHost resolves to #wk-deploy under redesign, #ops-content under legacy, both fed the same html string) and the reorder was not redesign-gated - both phantom-ship-gate and phantom-rd-reviewer flagged it as a Rule-11 (?legacy=1 byte-identical) violation. This version gates the reorder. CHANGE 1 (reflow, redesign-gated): in deploy_showRackDetail the Rack Elevation Hybrid block (OPEN AISLE door + rack-hybrid canvas/minimap) now builds into a local string rehHybridHtml instead of appending to html directly, and its double-rAF init (rackHybrid_initSync / rackFlat_applyFit / railSel_bindFlat / reh3d_restore) stays where it is - position-independent, it fires after the full html string is injected. The string is then emitted CONDITIONALLY on a new _rehPageRd (= redesign_isOn()) flag: under ?legacy=1 it is emitted in its ORIGINAL position (right after the block, before the Ghost Echo banner) so legacy is byte-identical to .353; under redesign it is emitted AFTER the NERVE card (which renders NEXT ACTION). Redesign page order is now header, rack nav strip, phase dots, COMPONENTS, Ghost Echo, NERVE (NEXT ACTION leads), 3D elevation, Phase Cards - option (a). Legacy order unchanged: COMPONENTS, elevation (flat), Ghost Echo, NERVE, Phase Cards. The block interior is otherwise byte-identical (only html += became rehHybridHtml +=). Ghost Echo and NERVE were NOT moved or altered. CHANGE 2 (tap-target): .reh-aisle-door gains min-height:44px + box-sizing:border-box + padding:0 16px (was ~25px) for the gloved 44px floor rd-review flagged on .353. The .reh-3d-toggle strip is display:flex/align-items:stretch, so the injected CABLES chip stretches to 44px too - fine, it is a real <button> so WebKit centers its content (rd-review .354 confirmed, not a bug). WHY .354 STACKS ON UNVERIFIED .353: per the CALL-0 BATCH-VERIFY protocol ships may stack and verify in one consolidated pass; both are owner-directed, land on the SAME rack-detail surface, and each is its own revertable commit (revert .354 -> .353's 3D-is-the-view, door at the smaller size, elevation back above NEXT ACTION under redesign). RACK SCENE LOCK: untouched - no lights/exposure/fog/tone-mapping/type-colour/geometry value changed; only html emission order (redesign) and one CSS rule. Gates: inline-JS vm.Script x3, sw.js compile, valid JSON, CSS brace net-0, three-stamp .353->.354, CRLF preserved. Rule-11 FIX verified: the reorder is redesign-gated, legacy keeps its original section order. DEVICE-VERIFY (clear SW first), BOTH shells (390 + 1024+): REDESIGN rack detail -> leads with header/nav/phase-dots/COMPONENTS, then NERVE (NEXT ACTION), then the 3D elevation BELOW it (was above); OPEN AISLE door is now ~44px tall; 3D still mounts, tap a device -> sheet, OPEN AISLE opens the aisle, CABLES/rail/EXPLODE present. ?legacy=1 rack detail -> UNCHANGED section order (elevation still above Ghost Echo/NERVE), pixel-identical to .353. ROLLBACK: revert this commit."
}
